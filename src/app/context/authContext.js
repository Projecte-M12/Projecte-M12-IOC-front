import { createContext } from "react";

/**
 * Context d'autenticació utilitzat per proporcionar informació d'autenticació a components de React.
 * @type {Context} Context d'autenticació
 */
export const authContext = createContext(null);
